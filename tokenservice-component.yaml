apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  annotations:
    backstage.io/kubernetes-label-selector: 'app=tokenservice'
  name: tokenservice
spec:
  lifecycle: production
  owner: Devops
  type: service
